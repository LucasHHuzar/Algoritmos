#include <stdio.h>
#include <locale.h>

int main (){
	
	setlocale (LC_ALL, "Portuguese");
	
	int contador, soma = 0;
	
	for (contador = 1; contador <= 100; contador++){
		soma += contador;
	}
	
	printf("A soma é: %d", soma);
	
	return 0;
}
