#include <stdio.h>
#include <string.h>

int main(){
	
	char str[31];
	int i;
	
	printf("Escreva uma frase: ");
	scanf(" %50[^\n]", str);
	
	for(i = 0; str[i] != '\0'; i++){
		if(str[i] != ' '){
			printf("%c", str[i]);
		}
	}
	
	return 0;
}
